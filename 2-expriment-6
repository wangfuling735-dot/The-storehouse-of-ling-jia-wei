#include<stdio.h>
    int x;
    int y;
    int main(){
    printf("请输入一个数，我将累加它与1之间的奇数");
    scanf("%d",&x);
    if (x%2==0){
        y=(x*x)/4;
        printf("结果为：%d\n",y);
    }
        else{
        y=((x+1)*(x+1))/4;
        printf("结果为：%d\n",y);
        }
    return 0;
    }
