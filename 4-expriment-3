#include<stdio.h>

    void swap (int*x,int*y)
{
    int temp =*x;
    *x=*y;
    *y=temp;

}


    int main ()
{

    int a,b;

    printf("请输入两个数,并且用逗号隔开\n");

    scanf("%d,%d",&a,&b);

    printf("原本为：a=%d,b=%d\n",a,b);

    swap(&a,&b);

    printf("转换后为：a=%d,b=%d\n",a,b);

    return 0;

}
